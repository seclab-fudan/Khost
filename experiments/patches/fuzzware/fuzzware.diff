diff -rNu '--exclude=.git' fuzzware/Dockerfile fuzzware-modified/Dockerfile
--- fuzzware/Dockerfile	2026-01-29 11:59:24.489062939 +0800
+++ fuzzware-modified/Dockerfile	2026-01-29 12:21:15.180415932 +0800
@@ -13,8 +13,8 @@
     echo "user ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers
 
 RUN USER=user GROUP=user VERSION=0.5.1 && \
-    wget https://github.com/boxboat/fixuid/releases/download/v$VERSION/fixuid-$VERSION-linux-amd64.tar.gz && \
-    tar -C /usr/local/bin -xzf fixuid-$VERSION-linux-amd64.tar.gz && \
+    wget https://github.com/boxboat/fixuid/releases/download/v$VERSION/fixuid-$VERSION-linux-arm64.tar.gz && \
+    tar -C /usr/local/bin -xzf fixuid-$VERSION-linux-arm64.tar.gz && \
     chown root:root /usr/local/bin/fixuid && \
     chmod 4755 /usr/local/bin/fixuid && \
     mkdir -p /etc/fixuid && \
@@ -48,8 +48,8 @@
 WORKDIR $FUZZWARE/emulator
 USER user
 RUN ./get_afl.sh && \
-    UNICORN_QEMU_FLAGS="--python=/usr/bin/python3" make -C $FUZZWARE/emulator/afl clean all && \
-    make -C $FUZZWARE/emulator/AFLplusplus clean all && \
+    UNICORN_QEMU_FLAGS="--python=/usr/bin/python3" AFL_NO_X86=1 make -C $FUZZWARE/emulator/afl clean all && \
+    AFL_NO_X86=1 make -C $FUZZWARE/emulator/AFLplusplus clean all && \
     cd $FUZZWARE/emulator/unicorn && \
     ./build_unicorn.sh
 
diff -rNu '--exclude=.git' fuzzware/emulator/get_afl.sh fuzzware-modified/emulator/get_afl.sh
--- fuzzware/emulator/get_afl.sh	2026-01-29 11:59:16.449195633 +0800
+++ fuzzware-modified/emulator/get_afl.sh	2026-01-29 11:47:41.156257849 +0800
@@ -36,5 +36,5 @@
 
 if [ ! -e AFLplusplus ]; then
     git clone https://github.com/AFLplusplus/AFLplusplus
-    git -C AFLplusplus checkout 3.14c
+    git -C AFLplusplus checkout b89727bea903aec80
 fi
diff -rNu '--exclude=.git' fuzzware/emulator/harness/fuzzware_harness/native/core_peripherals/cmsis/core_cm3.h fuzzware-modified/emulator/harness/fuzzware_harness/native/core_peripherals/cmsis/core_cm3.h
--- fuzzware/emulator/harness/fuzzware_harness/native/core_peripherals/cmsis/core_cm3.h	2026-01-29 11:59:16.457195500 +0800
+++ fuzzware-modified/emulator/harness/fuzzware_harness/native/core_peripherals/cmsis/core_cm3.h	2026-01-29 11:47:05.108286642 +0800
@@ -87,7 +87,7 @@
 
 #elif defined (__ARMCC_VERSION) && (__ARMCC_VERSION >= 6010050)
   #if defined __ARM_FP
-    #error "Compiler generates FPU instructions for a device without an FPU (check __FPU_PRESENT)"
+    // #error "Compiler generates FPU instructions for a device without an FPU (check __FPU_PRESENT)"
   #endif
 
 #elif defined ( __GNUC__ )
diff -rNu '--exclude=.git' fuzzware/emulator/harness/fuzzware_harness/native/Makefile fuzzware-modified/emulator/harness/fuzzware_harness/native/Makefile
--- fuzzware/emulator/harness/fuzzware_harness/native/Makefile	2026-01-29 11:59:16.453195567 +0800
+++ fuzzware-modified/emulator/harness/fuzzware_harness/native/Makefile	2026-01-29 11:47:05.112286639 +0800
@@ -6,7 +6,8 @@
 BIN_EXT = .so
 
 CC = clang
-CFLAGS += -fpic -Wall -Werror $(INC) -g -O3
+# CFLAGS += -fpic -Wall -Werror $(INC) -g -O3
+CFLAGS += -fpic -Wno-format $(INC) -g -O3
 LDFLAGS += -shared -L$(LIBDIR) -lunicorn
 
 .PHONY: all clean
diff -rNu '--exclude=.git' fuzzware/emulator/unicorn/fuzzware-unicorn/Makefile fuzzware-modified/emulator/unicorn/fuzzware-unicorn/Makefile
--- fuzzware/emulator/unicorn/fuzzware-unicorn/Makefile	2026-01-29 11:59:23.701075884 +0800
+++ fuzzware-modified/emulator/unicorn/fuzzware-unicorn/Makefile	2026-01-29 11:47:05.136286620 +0800
@@ -143,8 +143,10 @@
 ifeq ($(UNICORN_DEBUG),yes)
 CFLAGS += -g
 else
-CFLAGS += -O3 -g -DNDEBUG -march=native
-UNICORN_CFLAGS += -O3 -g -DNDEBUG -march=native
+# CFLAGS += -O3 -g -DNDEBUG -march=native
+# UNICORN_CFLAGS += -O3 -g -DNDEBUG -march=native
+CFLAGS += -O3 -g -DNDEBUG # -march=armv8-a -mfloat-abi=soft
+UNICORN_CFLAGS += -O3 -g -DNDEBUG # -march=armv8-a -mfloat-abi=soft
 UNICORN_QEMU_FLAGS += --disable-debug-info
 endif
 
diff -rNu '--exclude=.git' fuzzware/emulator/unicorn/fuzzware-unicorn/qemu/tcg/aarch64/tcg-target.c fuzzware-modified/emulator/unicorn/fuzzware-unicorn/qemu/tcg/aarch64/tcg-target.c
--- fuzzware/emulator/unicorn/fuzzware-unicorn/qemu/tcg/aarch64/tcg-target.c	2026-01-29 11:59:24.025070558 +0800
+++ fuzzware-modified/emulator/unicorn/fuzzware-unicorn/qemu/tcg/aarch64/tcg-target.c	2026-01-29 11:47:05.168286596 +0800
@@ -999,7 +999,11 @@
     TCGMemOp opc = lb->opc;
     TCGMemOp size = opc & MO_SIZE;
 
-    reloc_pc19(lb->label_ptr[0], s->code_ptr);
+    // reloc_pc19(lb->label_ptr[0], s->code_ptr);
+	const int type = (HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ) || HOOK_EXISTS(s->uc, UC_HOOK_MEM_WRITE) || HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ_AFTER)) ?
+	            R_AARCH64_JUMP26 : R_AARCH64_CONDBR19;
+	patch_reloc(lb->label_ptr[0], type, (intptr_t)s->code_ptr, 0);
+
 
     tcg_out_mov(s, TCG_TYPE_I64, TCG_REG_X0, TCG_AREG0);
     tcg_out_mov(s, TARGET_LONG_BITS == 64, TCG_REG_X1, lb->addrlo_reg);
@@ -1020,7 +1024,10 @@
     TCGMemOp opc = lb->opc;
     TCGMemOp size = opc & MO_SIZE;
 
-    reloc_pc19(lb->label_ptr[0], s->code_ptr);
+    // reloc_pc19(lb->label_ptr[0], s->code_ptr);
+	const int type = (HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ) || HOOK_EXISTS(s->uc, UC_HOOK_MEM_WRITE) || HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ_AFTER)) ?
+	            R_AARCH64_JUMP26 : R_AARCH64_CONDBR19;
+	patch_reloc(lb->label_ptr[0], type, (intptr_t)s->code_ptr, 0);
 
     tcg_out_mov(s, TCG_TYPE_I64, TCG_REG_X0, TCG_AREG0);
     tcg_out_mov(s, TARGET_LONG_BITS == 64, TCG_REG_X1, lb->addrlo_reg);
@@ -1104,7 +1111,15 @@
 
     /* If not equal, we jump to the slow path. */
     *label_ptr = s->code_ptr;
-    tcg_out_goto_cond_noaddr(s, TCG_COND_NE);
+    // tcg_out_goto_cond_noaddr(s, TCG_COND_NE);
+    /* Unicorn: fast path if hookmem is not enable */
+	if (!HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ) && !HOOK_EXISTS(s->uc, UC_HOOK_MEM_WRITE) && !HOOK_EXISTS(s->uc, UC_HOOK_MEM_READ_AFTER)){
+	    tcg_out_goto_cond_noaddr(s, TCG_COND_NE);
+	} else {
+	  /* If not equal, we jump to the slow path. */
+	    tcg_out_goto_noaddr(s);
+	}
+
 }
 
 #endif /* CONFIG_SOFTMMU */
diff -rNu '--exclude=.git' fuzzware/emulator/unicorn/fuzzware-unicorn/samples/Makefile fuzzware-modified/emulator/unicorn/fuzzware-unicorn/samples/Makefile
--- fuzzware/emulator/unicorn/fuzzware-unicorn/samples/Makefile	2026-01-29 11:59:24.049070164 +0800
+++ fuzzware-modified/emulator/unicorn/fuzzware-unicorn/samples/Makefile	2026-01-29 11:47:05.184286583 +0800
@@ -23,7 +23,7 @@
 LDLIBS += -lpthread -lunicorn -lm
 
 ifneq ($(CROSS),)
-CC = $(CROSS)gcc
+CC = $(CROSS)-gcc
 endif
 
 ifeq ($(UNICORN_ASAN),yes)
diff -rNu '--exclude=.git' fuzzware/modeling/requirements.txt fuzzware-modified/modeling/requirements.txt
--- fuzzware/modeling/requirements.txt	2026-01-29 11:59:11.937270692 +0800
+++ fuzzware-modified/modeling/requirements.txt	2026-01-29 11:55:43.117286865 +0800
@@ -5,3 +5,4 @@
 capstone==5.0.3
 angr==9.2.61
 IPython
+pycparser==2.22
